name: Deploy Board Service

on:
  push:
    branches: [main]
    paths:
      - 'board/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      fastapi: ${{ steps.filter.outputs.fastapi }}
      nginx: ${{ steps.filter.outputs.nginx }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            fastapi:
              - 'board/fastapi/**'
            nginx:
              - 'board/nginx/**'

  deploy-fastapi:
    needs: detect-changes
    if: needs.detect-changes.outputs.fastapi == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push board-fastapi
        run: |
          IMAGE=${{ secrets.DOCKERHUB_USERNAME }}/st1-board-fastapi:${{ github.sha }}
          docker build -t $IMAGE ./board/fastapi
          docker push $IMAGE

      - name: Update image tag in deploy.yaml
        run: |
          sed -i "s|image: .*/st1-board-fastapi:.*|image: ${{ secrets.DOCKERHUB_USERNAME }}/st1-board-fastapi:${{ github.sha }}|" k8s/board/deploy.yaml
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add k8s/board/deploy.yaml
          git diff --cached --quiet || (git commit -m "ci: update board-fastapi image to ${{ github.sha }}" && git pull --rebase && git push)

  deploy-nginx:
    needs: detect-changes
    if: needs.detect-changes.outputs.nginx == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push board-nginx
        run: |
          IMAGE=${{ secrets.DOCKERHUB_USERNAME }}/st1-board-nginx:${{ github.sha }}
          docker build -t $IMAGE ./board/nginx
          docker push $IMAGE

      - name: Update image tag in deploy.yaml
        run: |
          sed -i "s|image: .*/st1-board-nginx:.*|image: ${{ secrets.DOCKERHUB_USERNAME }}/st1-board-nginx:${{ github.sha }}|" k8s/board/deploy.yaml
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add k8s/board/deploy.yaml
          git diff --cached --quiet || (git commit -m "ci: update board-nginx image to ${{ github.sha }}" && git pull --rebase && git push)
